Have a method on CollectionFixture that locks all files in a dir so that 
export tests are pure. Don't have to worry about them reading the existing
files on disk.

EmitToMemory needs to be a theory that works on analyzer kinds
Rename CompilerLogState to LogReaderState
x Need to use CompilerLogReaderUtil in more of the using all compiler log tests

x CompilerCall
x   - ParseArguments should throw if it came from a compiler log
x   - make ownership a bit more clear



